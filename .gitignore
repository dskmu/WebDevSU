# === Core .NET build artifacts ===
# Keep these — they are generated by the build system
**/bin/
**/obj/
# VS solution-wide cache
.vs/

# Rider / JetBrains
.idea/
*.iml

# User-specific IDE files
*.suo
*.user
*.userosscache
*.sln.docstates
*.DotSettings.user

# Build configurations
**/Debug/
**/Release/
**/Published/
**/publish/

# Test outputs
TestResults/
*.trx
*.coverage*
.vscode/launch.json

# Logs & diagnostics
*.log
logs/
**/Logs/
DumpStack.log
*.dmp

# NuGet
*.nupkg
*.snupkg
packages/
# keep packages.config if you have it
!**/packages/*.keep

# Resharper / cache
_ReSharper*/
*_Resharper.*
*.resharper

# Azure publish profiles (user-scoped)
**/Properties/PublishProfiles/*.pubxml.user

# ASP.NET Core settings — avoid committing secrets
# Commit a sanitized example file instead (see negation below)
appsettings.json
appsettings.*.json
# allow example templates
!appsettings.example.json
!appsettings.*.example.json

# Entity Framework Core — keep Migrations (important!), but ignore design-time snapshot artifacts if any temp files appear
# (No rule required for Migrations by default — they will be tracked.)

# Frontend (for SPA / Vite / npm in src or ClientApp)
node_modules/
dist/
build/
# lock files are OK to commit if you want deterministic builds; otherwise ignore the next 3 lines
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Static site tools
.eslintcache

# LibMan cache (keep wwwroot/lib contents if you actually restore to it via libman.json)
#libman.json.cache

# OS noise
.DS_Store
Thumbs.db

# Dotnet tools / local secrets / misc
*.userprefs
*.local.json
.env
.env.*
secrets.json

# Coverage reports
coverage/
.coverage
.coverage.*
lcov.info
